package collector;

public class CompareResults {
	/*
	 * In compare, we have to deal with several cases
	 * 1) a file and a Directory haven't changed
	 * 2) The directory haven't changed, but the file have changed 
	 *    (same contents, but different line)
	 * 3) The directory have changed, but the file haven't changed
	 *    (same contents, but different directory)
	 * 4) The directory have changed, and the file also have changed
	 *    (same contents, but different directory and line)
	 */
	
}
